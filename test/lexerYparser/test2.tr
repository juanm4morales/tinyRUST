Str: _; // atributo
    // metodo para sumar
    // recursion??? /*    */
                             Token:token = get_Token();static fn ke(I32:n) -> I32{
        if (i==15){
            return nil;
        (IO.out_str(_);
        i = i + 1; // sad5as4d44¨*&&&%
        return ke(_);
    }

}
/* fin del
codigo
*/
