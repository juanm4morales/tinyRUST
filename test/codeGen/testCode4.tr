//

class A {
    I32: a;
}

fn main(){
    I32: sum;
    I32: a;

    Bool: b;
    a = 5;

    sum = ((2/2) + (3*4) + 1)/7;
    sum = sum + a;
    a = a * sum/4 + (8*4)%2;

    while (!b) {
        sum = sum - 1;
        if (sum<=0 && (b || true)) {
            b = true;
        }
    }

}