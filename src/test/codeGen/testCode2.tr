//

class A {
    I32: a;
}

fn main(){
    I32: sum;
    I32: a;
    Bool: b;
    a = 5;

    sum = ((2/2) + (3*4) + 1)/7;
    (IO.out_i32(sum));
    sum = sum + a;
    (IO.out_i32(sum));

    while (!b) {
        sum = sum - 1;
        if (sum<=0) {
            b = true;
        }
    }
    (IO.out_i32(sum));
    (IO.out_i32(sum));

}